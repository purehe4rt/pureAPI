package eu.pureheart.api.gui.menu;

import org.bukkit.Material;

public class MenuImpl extends Menu {

    public MenuImpl(String title, int rows, Material fillMaterial, boolean updateEnabled) {
        super(title, rows, fillMaterial, updateEnabled);
    }
}
